# PostgreSQL 配置
POSTGRES_USER=quant
POSTGRES_PASSWORD=quant_password_2024
POSTGRES_DB=qframe

# 数据库连接 URL (供应用使用)
DATABASE_URL=postgresql://quant:quant_password_2024@localhost:5432/qframe

# 异步连接 URL (供 asyncio 应用使用)
ASYNC_DATABASE_URL=postgresql+asyncpg://quant:quant_password_2024@localhost:5432/qframe

# pgAdmin 配置
PGADMIN_DEFAULT_EMAIL=admin@qframe.local
PGADMIN_DEFAULT_PASSWORD=admin_password_2024

# 时区
TZ=Asia/Shanghai
